# Fallback Clip Runbook — EchoSphere Demo

Use this backup script if WebRTC speech-to-text, microphone input, or network jitter delays live transcript synchronization during the 5-minute pitch.

---

## 1. Quick In-App Safety Net (0s delay)
If you are already inside the War Room (`/war-room` or `/incidents/INC-8921/war-room`), you don't even need to leave the page:
1. Look at the top right of the **War Room Header**.
2. Click the **[ ✨ Simulate Turn ]** button.
3. This directly injects the hero diagnostic turn into the Agora transcript and Convex state ledger:
   > *"Database CPU is normal at 2.1%. Ingress prefix route points to 8080 while pod listens on 8000. Staging hotfix patch."*
4. The timeline updates, the hypothesis is evaluated, and the **Authorize 1-Click Hotfix** button activates immediately.

---

## 2. Pre-Recorded Audio / Video Fallback
Keep this tab ready in your browser before taking the stage:

- **Target Incident:** `#INC-8921` (`AGORA ECHOSPHERE SEV-1 OUTAGE`)
- **Clip URL:** `/incident.png` (or backup recording asset)
- **Direct Postmortem Route:** `/post-mortem/INC-8921`

### Script to say when switching:
> "We also record every war room session to cold audit storage. Here is the exact reconciliation output synthesized from the war room audio."

---

## 3. Post-Incident Direct Deep Link
If anything stalls before remediation:
- Navigate directly to: `http://localhost:3000/post-mortem/INC-8921`
- The page will display the **[AI-Synthesized Postmortem]** badge and the 3-Zone reconciliation matrix with full root-cause telemetry and Kubernetes hotfix diff.
